# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0        # ASGI server that runs the FastAPI app
python-multipart==0.0.20         # lets FastAPI accept file uploads (admin panel)
pydantic==2.10.4                 # request/response validation
pydantic-settings==2.7.1         # loads config.py Settings from .env

# --- Auth ---
python-jose[cryptography]==3.3.0 # encode/decode the admin JWT
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- RAG stack (report Section 3.4.2) ---
google-genai==0.8.0              # gemini-embedding-001 -> 3072-dim vectors
qdrant-client==1.12.2            # vector database client, HNSW + cosine
cohere==5.13.6                   # command-r generator LLM

# --- Ingestion ---
PyMuPDF==1.25.1                  # PDF text extraction (imported as `fitz`)
beautifulsoup4==4.12.3           # strip HTML tags into readable text
lxml==5.3.0                      # fast parser BeautifulSoup uses
aiohttp==3.11.11                 # async HTTP fetching (scraper + URL ingest)
playwright==1.49.1               # JS-rendered pages + (future) ERP automation
Pillow==11.1.0

# --- Utils ---
tenacity==9.0.0
python-dotenv==1.0.1
httpx==0.28.1

# --- Optional scaling backend (services/cache.py, ratelimit.py) ---
# Only used if REDIS_URL is set in .env; falls back to in-memory otherwise.
redis==5.2.1

# --- Observability (app/logging_config.py, app/middleware.py, services/metrics.py) ---
prometheus-client==0.21.1        # /metrics endpoint — request/cache/circuit-breaker counters
